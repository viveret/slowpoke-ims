@model SlowPokeIMS.Web.ViewModels.IPaginatedEnumerableViewModel
@{
    var lasti = Model.Offset + 1;
}
<div class="d-grid fd-column g32 py16">
    @foreach (var item in Model.Items)
    {
        var i = lasti;
        <div class="grid--item">
            <span>#@string.Format("{0:n0}", i)</span>
            <partial name="Paginated/LargeItemList_Item_@item.GetType().Name" model="@item"/>
        </div>
        lasti++;
    }
</div>